{
  "name": "YEM SaaS — Local",
  "_postman_variable_scope": "environment",
  "values": [
    { "key": "base_url",         "value": "http://localhost:8080", "enabled": true },
    { "key": "token_admin",      "value": "", "enabled": true,
      "description": "Paste JWT from POST {{base_url}}/auth/login with admin@demo.ma" },
    { "key": "token_manager",    "value": "", "enabled": true,
      "description": "Paste JWT from POST {{base_url}}/auth/login with manager@demo.ma" },
    { "key": "token_agent",      "value": "", "enabled": true,
      "description": "Paste JWT from POST {{base_url}}/auth/login with agent@demo.ma" },
    { "key": "token_sa",         "value": "", "enabled": true,
      "description": "SUPER_ADMIN token (from bootstrap or superadmin@yem.ma login)" },
    { "key": "societe_id",       "value": "", "enabled": true,
      "description": "UUID of the company — from GET {{base_url}}/api/admin/societes" },
    { "key": "contact_id",       "value": "", "enabled": true,
      "description": "UUID of a contact — from GET {{base_url}}/api/contacts" },
    { "key": "membre_id",        "value": "", "enabled": true,
      "description": "UUID of a team member — from GET {{base_url}}/api/mon-espace/utilisateurs" },
    { "key": "invitation_token", "value": "", "enabled": true,
      "description": "Token received in invitation email — from POST /api/mon-espace/utilisateurs" }
  ]
}
