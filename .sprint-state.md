=== SPRINT STATE — Wave 10: E2E Coverage + Docs + Portal ===
Phase: 10 — E2E CI Fix + Portal E2E + Activation UX + Full Docs
Status: COMPLETE
Branch: Epic/Sales-Portal

Completed features:
  W10-1  Playwright project matching: added activation-tests, pipeline-tests, portal-tests projects to playwright.config.ts
  W10-2  activation.spec.ts: fixed URL from /activation/:token → /activation?token=; mocked valid-invitation with page.route(); asserts form visible, submit disabled, expiry note shown
  W10-3  pipeline.spec.ts: all page.request calls prefixed with ${API_BASE} (PLAYWRIGHT_API_BASE env var)
  W10-4  portal.spec.ts: 8 tests for buyer portal (login page, magic-link request, invalid token, unauthenticated redirect, ventes list with stepper, echeances, invite API, invite UI)
  W10-5  e2e.yml: PLAYWRIGHT_API_BASE=http://localhost:8080 env var injected into E2E test step
  W10-6  Activation password strength meter: 4-segment colour bar + per-requirement checklist (component getters, no regex in template)
  W10-7  Angular template fix: regex literals moved to component getters (pwHasLength/Upper/Lower/Digit/Special, passwordStrength, passwordStrengthLabel, passwordStrengthClass)

Wave 9 (complete, 2026-04-05):
  W9-1   Backend: VenteService.create() auto-advances contact to ACTIVE_CLIENT
  W9-2   Backend: VenteService.updateStatut() auto-advances contact to COMPLETED_CLIENT on LIVRE
  W9-3   Backend: VenteResponse.contactFullName (denormalised)
  W9-4   Backend: GET /api/ventes?contactId= filter via findByContactId()
  W9-5   Frontend: vente.service.ts — contactFullName, listByContact(), uploadDocument()
  W9-6   Frontend: vente-list — Acquéreur column with link to contact detail
  W9-7   Frontend: vente-detail — Acquéreur link, document upload UI
  W9-8   Frontend: contact-detail — Ventes tab (lazy-loaded)
  W9-9   Frontend: HomeDashboardComponent — KPI row, shortcut grid, recent ventes table
  W9-10  Frontend: app.routes.ts — default CRM route → dashboard, default portal route → ventes

Docs created:
  docs/user-guide/guide-agent.md       — French guide for CRM agents
  docs/user-guide/guide-manager.md     — French guide for managers
  docs/user-guide/guide-acquereur.md   — French guide for buyers (portal)
  docs/context/api-map.md              — Complete endpoint inventory (all modules)
  docs/context/known-issues.md         — Resolved and open bugs with root causes
  docs/ai/quick-context.md             — AI quick-start guide (load first)
  docs/ai/deep-context.md              — AI deep-dive (module inventory, patterns, IT test rules)
  docs/ai/prompt-playbook.md           — AI task recipes (common operations, debugging)

Test counts:
  E2E: 35 tests across 8 spec files (up from 15 in 5 files at Wave 9 start)
  Backend unit: 82 tests (no backend changes in Wave 10)
  Backend IT: unchanged

---
Context for next session (5 lines):
Wave 10 COMPLETE. 35 E2E tests (8 files); all spec files matched by Playwright projects. Backend: 25 modules, 58 Liquibase changesets (next: 059). Frontend: Angular 19, standalone components, @if/@for control flow. Key credential: admin@acme.com/Admin123!Secure. E2E API calls use PLAYWRIGHT_API_BASE=http://localhost:8080 in CI. Next: Wave 11 (TBD).

---
=== SPRINT STATE — Phase 8: Wave 3 Frontend + E2E + CI Hardening ===
Phase: 8 — Wave 3 Frontend Completion + E2E Tests + CI Hardening
Status: COMPLETE
Branch: Epic/users-management

Completed features:
  W3-1   Tasks Angular UI: TasksComponent (/app/tasks), TaskFormComponent, TaskService (CRUD + filters + pagination)
  W3-1b  Tasks sidebar nav link added to shell.component.html
  W3-1c  data-testid="create-task" and local-insert onSaved() pattern (no reload round-trip)
  W3-2   Documents Angular UI: DocumentsComponent, DocumentService (upload/list/download/delete)
  W3-3   Admin Users UI upgraded to new UserManagement API (/api/mon-espace/utilisateurs)
  W3-4   Playwright E2E suite: 14 tests across auth, contacts, tasks, superadmin specs — all green
  W3-4b  data-testid attributes: email, password, login-button, error-message (login form)
  W3-4c  data-testid attributes: logout-button (shell), create-contact, firstName, lastName, save-button (contacts)
  W3-4d  superadmin.spec.ts credentials aligned to changeset 046 (superadmin@yourcompany.com / YourSecure2026!)
  W3-4e  playwright.config.ts: workers=1 to serialize login requests (prevents rate-limit races)

CI fixes:
  CI-1   Removed @Transactional from all IT test classes (conflicts with AuditEventListener REQUIRES_NEW)
  CI-2   Unique email UIDs per IT test (prevents state leakage between tests)
  CI-3   AdminUserController moved /api/admin/users → /api/users (was blocked by SUPER_ADMIN security rule)
  CI-4   CrossSocieteIsolationIT: fixed $.page.totalElements JSON path
  CI-5   TlsConfigIT: disable all default health indicators, enable only ping
  CI-6   Angular tooling realigned to ^19.2.0 across all @angular-eslint/* packages (fixes chokidar npm ci failure)
  CI-7   Jasmine 5.x spy property fix: use closure variable instead of Object.defineProperty (configurable: false)
  CI-8   ESLint eqeqeq fix in societe-detail.component.html

Infrastructure fixes:
  INF-1  docker-compose.yml: JWT_SECRET fallback (:-) prevents @NotBlank Spring validation failure with empty env
  INF-2  nginx.frontend.conf: resolver 127.0.0.11 + $backend variable prevents nginx startup DNS crash
  INF-3  hlm-frontend/Dockerfile: curl -f healthcheck (was wget, unreliable in BusyBox)
  INF-4  .github/workflows/e2e.yml: inject .env with JWT_SECRET; docker compose up --wait --wait-timeout 180
