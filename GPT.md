# Working Agreement for GPT/Codex

- Read `docs/ai/quick-context.md` before making changes.
- Ground all claims in code or configs and cite file paths in responses.
- Prefer minimal, scoped patches over broad refactors.
- Never output or commit secrets (use placeholders).
- Use structured outputs (files inspected, plan, changed files, diff summary, tests, open questions).
