databaseChangeLog:
  - changeSet:
      id: 021-create-payment-call
      author: yem
      comment: >
        v1 payment_call table — retired.
        Superseded by payment_schedule_item in changeset 020 (v2 payments module).
        No-op placeholder to preserve changelog sequence.
      changes:
        - sql:
            sql: "SELECT 1 -- v1 payment_call retired"
