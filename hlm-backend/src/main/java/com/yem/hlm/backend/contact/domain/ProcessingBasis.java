package com.yem.hlm.backend.contact.domain;

/** Legal basis for processing this contact's personal data (GDPR Art. 6 / Law 09-08 Art. 4). */
public enum ProcessingBasis {
    CONTRACT,
    CONSENT,
    LEGITIMATE_INTEREST
}
