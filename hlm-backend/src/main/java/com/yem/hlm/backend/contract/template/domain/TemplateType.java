package com.yem.hlm.backend.contract.template.domain;

/**
 * Types of PDF documents whose Thymeleaf templates can be customised per société.
 */
public enum TemplateType {
    CONTRACT,
    RESERVATION,
    CALL_FOR_FUNDS
}
