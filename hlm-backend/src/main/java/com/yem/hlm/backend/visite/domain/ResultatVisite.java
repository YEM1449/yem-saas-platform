package com.yem.hlm.backend.visite.domain;

/** Outcome recorded when a {@link Visite} reaches {@code REALISEE} (RG-V06). */
public enum ResultatVisite {
    INTERESSE,
    A_RELANCER,
    PAS_INTERESSE,
    OPPORTUNITE_CREEE
}
