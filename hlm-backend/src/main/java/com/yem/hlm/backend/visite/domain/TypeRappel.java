package com.yem.hlm.backend.visite.domain;

/** When a {@link VisiteRappel} fires relative to the visit (RG-V07). */
public enum TypeRappel {
    /** 24 hours before the visit. */
    H24,
    /** 1 hour before the visit. */
    H1
}
