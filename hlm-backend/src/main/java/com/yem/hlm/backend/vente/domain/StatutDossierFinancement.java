package com.yem.hlm.backend.vente.domain;

/** Lifecycle of a financing file (dossier de financement). */
public enum StatutDossierFinancement {
    EN_COURS,
    ACCORD_PRINCIPE,
    ACCORD_DEFINITIF,
    REFUSE
}
